name: "Issue check"

on:
  issues:
    types:
      - reopened
      - opened

jobs:
  check-issue:
    uses: surfstudio/flutter-ci-workflows/.github/workflows/heck_issues.yaml@main
    with:
      REPO_PATH: 'https://github.com/surfstudio/flutter-bottom-inset-observer/issues/new/choose'
      ISSUE_TITLE: ${{ github.event.issue.title }}
      ISSUE_BODY: ${{ github.event.issue.body }}
