name: keyboard_listener
version: "2.0.0"
description: A Flutter plugin that helps to listen keyboard state (if it's visible or hidden)
repository: "https://github.com/surfstudio/flutter-keyboard-listener"
issue_tracker: "https://github.com/surfstudio/flutter-keyboard-listener/issues"
publish_to: none

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  surf_lint_rules: ^1.0.0
  

environment:
  sdk: ">=2.12.0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: keyboard_listener
  hostUrl:
  separate_repo_url:
